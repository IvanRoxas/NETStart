# UI Notification Rule
From now on, all localhost:3000 messages or choices (such as success, error, warning messages or simple confirmations) should be displayed using custom UI modals or toast notifications instead of standard browser alerts or confirms.
