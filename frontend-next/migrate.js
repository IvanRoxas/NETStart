const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const users = await prisma.user.findMany({ where: { displayName: null } }); console.log('Found ' + users.length + ' users without a displayName.'); for (const user of users) { const randomNum = Math.floor(10000 + Math.random() * 90000); const newDisplayName = 'Explorer' + randomNum; await prisma.user.update({ where: { id: user.id }, data: { displayName: newDisplayName } }); console.log('Updated user ' + user.id + ' with displayName: ' + newDisplayName); } } main().catch(console.error).finally(() => prisma.$disconnect());
